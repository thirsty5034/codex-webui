/**
 * @deprecated This file is no longer the app entry point.
 * Routing is now handled by TanStack Router — see routes/router.tsx.
 * Kept only to avoid breaking any stale imports during transition.
 */
export {};
